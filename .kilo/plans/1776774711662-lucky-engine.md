## Goal
Validate whether the downstream MSVC biosig patch queue applies cleanly to biosig `v3.9.5`, and if successful, update the `src/biosig` submodule pointer plus all pinned-tag references/scripts/docs to `v3.9.5`.

## Current findings (read-only)
- Superproject currently points `src/biosig` at `78e5cc51da8dfe4aa45b46420d25488a72a80d76` (`v3.9.3`).
- Patch queue is `cmake/patches/biosig-msvc/0001-b4c-msvc-enable-Visual-Studio-2022-builds.patch`.
- Multiple files hard-pin expected tag `v3.9.3`:
  - `cmake/PrepareBiosigMSVC.cmake`
  - `cmake/StimfitBiosigExternal.cmake`
  - `doc/biosig-msvc-maintainer.md`
- Remote biosig has `v3.9.5` (`318cf1efcefec4645fcc38a22e52cde5833c2a4c`), but local tag ref is not present yet.
- Between `v3.9.3` and `v3.9.5`, `biosig4c++/biosig.c` changed upstream (also touched by our patch), so a clean apply is possible but must be verified.

## Implementation plan
1. **Sync biosig tag metadata**
   - Fetch tags in `src/biosig` (`git -C src/biosig fetch --tags origin`).
   - Confirm `v3.9.5` resolves locally and capture commit hash.

2. **Test patch applicability against `v3.9.5`**
   - Use a disposable checkout context for `v3.9.5` (either temporary worktree or the existing staging flow).
   - Run patch apply check using same options as project scripts (`git apply --3way --ignore-space-change --ignore-whitespace --whitespace=nowarn`).
   - If patch fails, stop pointer update and report exact failing hunks/files.

3. **If patch applies: update submodule pointer**
   - Checkout `v3.9.5` in `src/biosig` and stage superproject submodule pointer change.

4. **Update pinned-version scripts/docs**
   - Replace `v3.9.3` -> `v3.9.5` in:
     - `cmake/PrepareBiosigMSVC.cmake`
     - `cmake/StimfitBiosigExternal.cmake`
     - `doc/biosig-msvc-maintainer.md` (narrative + `format-patch` example + option docs)
   - Verify no remaining stale references to `v3.9.3` in Windows patched-biosig flow files.

5. **Validation checks**
   - Re-run the patch preparation workflow command: `cmake -P cmake/PrepareBiosigMSVC.cmake`.
   - Confirm it passes tag guard and patch application.
   - Optionally run configure step for patched preset to ensure integration still resolves.

6. **Review outcome**
   - Summarize whether patch applies on `v3.9.5`.
   - If yes: list files changed and new submodule SHA.
   - If no: provide conflict summary and recommended patch refresh path.

## Execution notes
- Do not alter unrelated repository changes.
- Keep patch queue unchanged unless a conflict forces a patch refresh.
- Only proceed with pointer/tag updates on successful patch application check.
